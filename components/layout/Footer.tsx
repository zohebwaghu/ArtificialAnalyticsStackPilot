export function Footer() {
  return (
    <footer className="mt-14 border-t border-[var(--line)] py-7 font-mono text-[10px] uppercase text-[var(--muted)]">
      <div className="shell flex flex-wrap justify-between gap-3 !pb-0">
        <span>StackPilot / decision support for AI builders</span>
        <span>Curated snapshot · verify before production use</span>
      </div>
    </footer>
  );
}
