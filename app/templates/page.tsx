import Link from "next/link";
import { templates } from "@/lib/data-loader";

export default function TemplatesPage(){ return <main className="shell py-10"><p className="eyebrow">Project blueprints</p><h1 className="mt-2 text-4xl font-bold">Start from a proven shape</h1><p className="mt-3 max-w-2xl text-[var(--muted)]">Each blueprint seeds the recommendation engine and can be customized in the stack wizard.</p><div className="mt-8 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{templates.map((template) => <article className="panel p-5" key={template.id}><div className="flex justify-between"><span className="tag">{template.difficulty}</span><span className="mono text-[10px] text-[var(--green)]">{template.estimated_time}</span></div><h2 className="mt-6 text-xl font-bold">{template.name}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-[var(--muted)]">{template.description}</p><div className="mt-4 flex flex-wrap gap-1">{template.skills_required.map((skill) => <span className="tag" key={skill}>{skill}</span>)}</div><Link href={`/build?type=${template.id}`} className="btn mt-6 w-full">Use blueprint →</Link></article>)}</div></main>; }
